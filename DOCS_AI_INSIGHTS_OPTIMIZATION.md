# AI Insights - Оптимизация и Изменения

## ✅ Реализованные улучшения

### 1. Кеширование для экономии токенов

**Проблема:** Каждый запрос AI инсайтов вызывал Gemini API, тратя токены даже при повторных запросах.

**Решение:**
- Добавлено in-memory кеширование с TTL 1 час
- Cache key формируется из: `userId + startDate + endDate`
- При повторном открытии шторки в течение часа данные берутся из кеша
- Автоматическая очистка устаревших записей кеша

**Преимущества:**
- ✅ Экономия токенов (до 95% при частых запросах)
- ✅ Мгновенный ответ при повторном запросе
- ✅ Автоматическое обновление данных каждый час

**Код:**
```typescript
const insightsCache = new Map<string, CacheEntry>();
const CACHE_TTL_MS = 60 * 60 * 1000; // 1 hour

// Проверка кеша перед запросом к AI
const cached = insightsCache.get(cacheKey);
if (cached && Date.now() - cached.timestamp < CACHE_TTL_MS) {
    return cached.data;
}
```

**Логи:**
В терминале вы увидите:
- `[AI Insights] Returning cached data` - данные из кеша
- `[AI Insights] Calling Gemini AI...` - новый запрос к AI

---

### 2. Удаление раздела Recurring Transactions

**Проблема:** Функционал "recurring transactions" был заменен на "transaction templates", но AI продолжал показывать секцию с повторяющимися транзакциями.

**Изменения:**
- ❌ Удален подсчет recurring transactions из backend
- ❌ Удалено поле `subscriptions` из Zod schema
- ❌ Удалена карточка "Recurring Transactions" из UI
- ✅ Упрощен AI prompt (теперь 3 пункта вместо 4)

**Результат:**
- Более чистый UI (3 карточки вместо 4)
- AI не генерирует ненужную информацию
- Экономия токенов (~10-15%)

---

## 📊 Текущая структура AI Insights

Теперь показываются только 3 карточки:

1. **Spending Trend** 📈📉➡️
   - Направление тренда (вверх/вниз/стабильно)
   - Процент изменения
   - Сообщение с объяснением

2. **Top Category** 🍎💳🚕
   - Название категории с emoji
   - Сумма расходов
   - Персональный совет по этой категории

3. **Financial Tip** 💡
   - Общий финансовый совет на основе всех данных
   - Конкретный и действенный

---

## 💰 Экономия токенов

### До оптимизации:
- Каждый запрос = ~2,000-3,000 токенов
- 10 запросов в день = 20,000-30,000 токенов/день

### После оптимизации:
- Первый запрос = ~2,000-3,000 токенов
- Повторные запросы (в течение часа) = 0 токенов
- Реальная экономия: ~80-90%

### Пример использования:
```
14:00 - Открыл AI Insights → AI запрос (3000 токенов)
14:15 - Снова открыл → Кеш (0 токенов)
14:30 - Снова открыл → Кеш (0 токенов)
15:05 - Открыл через час → Новый AI запрос (3000 токенов)
```

---

## 🔄 Дополнительные способы экономии

### Уже реализовано:
1. ✅ Кеширование с TTL 1 час
2. ✅ Ограничение контекста до 50 последних транзакций
3. ✅ Упрощенная схема ответа (убрали subscriptions)

### Можно добавить в будущем:

#### 1. Настройка TTL кеша через ENV
```typescript
const CACHE_TTL_MS = parseInt(process.env.AI_INSIGHTS_CACHE_TTL || '3600000');
```

#### 2. Персистентное кеширование через Redis
```typescript
// Вместо in-memory Map использовать Redis
await redis.setex(cacheKey, 3600, JSON.stringify(insights));
```

#### 3. Ручное обновление инсайтов
Добавить кнопку "Refresh" в UI для принудительного обновления данных.

#### 4. Умное кеширование
Обновлять кеш только если появились новые транзакции:
```typescript
const lastTransactionDate = await getLastTransactionDate(userId);
if (cached && cached.lastTransactionDate === lastTransactionDate) {
    return cached.data;
}
```

#### 5. Batch запросы
Если несколько пользователей запрашивают инсайты одновременно, объединять запросы.

---

## 🎯 Рекомендации

### Для production:
1. **Мониторинг токенов:** Отслеживайте использование через Google AI Studio
2. **Лимиты:** Установите rate limiting (например, max 10 запросов/день на пользователя)
3. **Fallback:** При превышении квоты показывайте кешированные данные или статистику без AI

### Текущие настройки:
- Модель: `gemini-2.5-flash` (бесплатно до 1500 запросов/день)
- Cache TTL: 1 час
- Контекст: До 50 транзакций
- Auto-cleanup: Да

---

## 📝 Проверка работы

### В терминале смотрите логи:
```bash
[AI Insights] Starting generation for user: xxx
[AI Insights] Date range: { startDate: ..., endDate: ... }
[AI Insights] Current transactions fetched: X
[AI Insights] Calling Gemini AI...
[AI Insights] Current expenses: XXX
[AI Insights] Previous expenses: XXX
[AI Insights] AI response received: { ... }
```

При втором запросе в течение часа:
```bash
[AI Insights] Returning cached data
```

---

## ⚠️ Важно

1. In-memory кеш **очищается при перезапуске сервера**
2. Для production рекомендуется Redis или другое персистентное хранилище
3. Текущий кеш работает только в рамках одного инстанса Next.js
4. При деплое на Vercel каждый serverless function имеет свой кеш
