AI chat

2. Чеклист для AI-IDE: Глобальный AI Чат (Mobile & Desktop)
Ты правильно решил делать сессии (список бесед). Это стандарт индустрии (ChatGPT, Claude). Пользователь хочет обсудить "Отпуск" в одном чате, а "Покупку машины" в другом, чтобы контекст не смешивался.
Скопируй этот блок для своей модели:
🏗 Database Schema (Prisma/SQL)
 * [ ] Create ChatSession table:
   * id (UUID), userId, title (Auto-generated by AI or first message), createdAt.
 * [ ] Create ChatMessage table:
   * id, sessionId (Relation), role (user/assistant/system), content (Text), toolCalls (JSON for widgets), createdAt.
📱 Layout & Navigation (Responsive)
 * [ ] Route Setup: Создать страницу /chat (или /ai) которая доступна из Сайдбара (Desktop) и Таб-бара (Mobile).
 * [ ] Layout Structure:
   * Desktop: Two-column layout.
     * Left Sidebar (250px): Список прошлых чатов (ChatHistoryList) + Кнопка "New Chat".
     * Main Area: Окно переписки.
   * Mobile: Full-screen chat.
     * Список чатов скрыт в Sheet (Drawer), который открывается по кнопке "История" (иконка часов) в Хедере.
🎨 Chat UI Components
 * [ ] Chat Header:
   * Left: Кнопка "Меню/История" (Mobile only).
   * Center: Название модели/Ассистента.
   * Right: Tone Selector (Dropdown/Badge).
     * Logic: Добавить проверку if (!user.isPro) блокировать выбор и показывать тултип "Upgrade to Pro".
 * [ ] Message Area:
   * Markdown Support: Использовать react-markdown для рендеринга ответов (bold, lists, tables).
   * Date Separators: Рендерить компонент "Дата" (Сегодня/Вчера) между группами сообщений.
   * User Bubble: Align right, Brand Color (Blue/Primary).
   * AI Bubble: Align left, Gray/Muted background, с Аватаром.
   * Loading State: Анимация "Прыгающие точки" (Typing indicator) во время ожидания ответа.
 * [ ] Empty State (New Chat):
   * Показывать Grid (2x2) с карточками-пресетами по центру экрана.
   * При клике на пресет — отправлять сообщение и создавать новую сессию.
 * [ ] Active State (Suggestions):
   * Показывать горизонтальный скролл (ScrollArea) с "чипсами" (короткие вопросы) над полем ввода, если есть история переписки.
⚙️ Logic
 * [ ] Streaming: Реализовать потоковую передачу текста (Vercel AI SDK useChat), чтобы ответ печатался по буквам.
 * [ ] Auto-Title: После первого сообщения юзера отправлять фоновый запрос к AI: "Generate a 3-4 word title for this conversation". Обновлять название сессии в БД.