Чеклист работ: модалки транзакций и бюджетов

1. Модалка транзакции — порядок и акценты
- [x] Переместить поле `Amount` в самый верх.
- [x] Увеличить размер шрифта суммы до `text-3xl` или `text-4xl`.

2. Модалка транзакции — переключатель типа
- [x] Заменить радио-кнопки на `Tabs`/Segmented Control: `[Расход | Доход]` вверху.
- [x] По умолчанию выбрать `Расход`.
- [x] Визуальный отклик: для `Расход` — красная сумма/кнопка, для `Доход` — зеленая.

3. Модалка транзакции — поле Названия
- [x] Добавить `Autocomplete/Combobox` для поиска по истории/правилам.
- [x] При выборе шаблона (например, `Netflix`) автоматически заполнять сумму и категорию.

4. Модалка транзакции — автокатегория
- [x] На `onBlur` поля названия проверять локальный словарь/историю.
- [x] Автоматически выбирать категорию при найденном совпадении.

5. Модалка транзакции — действия
- [x] Убрать нижнюю кнопку `Отмена` (оставить `X` в хедере модалки).
- [x] Кнопку `Сохранить/Добавить` сделать `w-full` и закрепить снизу (`sticky`).

6. Модалка транзакции — фокус и ввод
- [x] При открытии модалки фокус ставить на поле суммы.
- [x] Задать цифровую клавиатуру: `inputmode="decimal"`.

7. Автокомплит — подход
- [x] Реализовать только клиентскую фильтрацию (без AI).
- [x] Использовать `Combobox` или `Command` из `shadcn/ui` при наличии, либо аналогичный легковесный компонент.

8. Модалка бюджета — порядок полей
- [x] Переключатель типа: `Расход` / `Цель накопления`.
- [x] Название + эмодзи: пикер эмодзи слева, инпут справа.
- [x] Лимит/Цель (сумма): динамический лейбл — «Лимит на месяц» (для расхода) или «Цель» (для накопления).
- [x] Выбор цвета: `Color picker/Swatches`.

9. Модалка бюджета — UI-компоненты
- [ ] Использовать компоненты `shadcn/ui` (например, `Input`, `Label`, `Popover` для цвета/эмодзи), если библиотека присутствует в проекте.