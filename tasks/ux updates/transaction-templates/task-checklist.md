Чеклист работ: переход на Шаблоны транзакций

1. Модель данных и переименование
- [x] Переименовать сущность «Rules» в «Templates/TransactionTemplates» в коде и UI (без затрагивания Recurring Rules для подписок).
- [x] Добавить/обновить таблицу `transaction_templates`: поля — `title`, `amount`, `budget_folder_id`, `type` ('expense'|'income'); удалить поле `date` из схемы шаблонов.
- [x] Реализовать проверку лимита для Free: максимум 3 шаблона; при превышении — локализованный тост/модалка.

2. Transaction Modal — ввод и автозаполнение
- [x] Заменить обычный инпут названия на `Combobox/Command` с локальной фильтрацией.
- [x] Источник данных: список шаблонов пользователя (помечать ⭐) + история уникальных названий.
- [x] Логика выбора: при клике по шаблону — авто-заполнение `Amount`, `Category (budget_folder_id)`, `Title`, `Type`; `Date` остаётся «Сегодня».
- [x] Добавить переключатель `Save as template` (Switch/Checkbox) у нижней границы формы.
- [x] Сабмит: всегда создавать транзакцию; если переключатель включён — дополнительно создать шаблон (с проверкой лимита).

3. Settings — управление шаблонами
- [x] Переименовать раздел в «Шаблоны транзакций»/«Избранное».
- [x] В форме редактирования шаблона убрать поле выбора даты.

4. Оптимизация сценария ввода
- [x] Удалить старую логику с Confirm/Undo при полном совпадении названия; автозаполнение происходит сразу при выборе пункта из списка.
- [x] Визуально выделять элементы-шаблоны в списке (иконка ⭐ или жирный шрифт) для отличия от истории.

5. Локализация
- [x] Добавить/обновить ключи: «Шаблоны транзакций», «Сохранить как шаблон», сообщения о лимите, элементы управления в Settings.

6. Права и тарифы
- [x] Проверка плана (Free/Pro) при создании шаблонов и показ соответствующих подсказок.

7. Технические детали
- [x] Типизация: строгие типы для шаблонов и их применения (title, amount, type, budget_folder_id).
- [ ] Телеметрия (опционально): трекать выбор шаблонов и создание новых.