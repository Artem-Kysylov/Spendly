Transactions templates
1. Смена концепции: «Правила» -> «Шаблоны» (Templates)
Слово «Правило» звучит как что-то, что выполняется автоматически без участия юзера (backend logic).
То, что ты описываешь — это Шаблоны или Избранное.
Разделение:
 * Recurring (Рекуррентные): Есть жесткая дата (12-е число). Происходят автоматически. Юзер их не вводит руками каждый раз. (Это отдельная фича, пока отложим).
 * Templates (Шаблоны): Даты нет (или она всегда "Сегодня"). Сумма и Категория зафиксированы. Юзер вводит их руками, но быстро.
2. Где создавать и хранить Шаблоны?
Заставлять юзера идти в Настройки -> Создавать правило -> Потом идти в Транзакции — это слишком длинный путь.
Лучшее место — прямо в момент создания транзакции (Organic Creation).
 * Как это выглядит:
   В Модалке Добавления Транзакции, в самом низу (над кнопкой Сохранить), добавь маленький переключатель (Switch) или чекбокс:
   [ ] Сохранить как шаблон
 * Логика:
   * Юзер вводит: Netflix, 20$, Подписки.
   * Ставит галочку [v] Сохранить как шаблон.
   * Нажимает "Сохранить".
   * Транзакция добавляется в историю И сохраняется в список шаблонов.
 * Ограничение (Free Plan): Если у юзера уже 3 шаблона и он ставит галочку — показывай красивый тост/модалку: "В бесплатной версии доступно 3 шаблона. Перейдите на Premium или удалите старые в настройках".
3. Где редактировать?
Оставь список в Настройках, как сейчас. Это нормальное место для "уборки" (удалить старое, переименовать).
Только переименуй раздел в «Шаблоны транзакций» или «Избранное».
4. Поля для Шаблона (Упрощаем)
Из настроек шаблона нужно убрать поле «Дата».
 * Почему: Шаблоны используются для ускорения ручного ввода здесь и сейчас. Дата шаблона всегда должна быть currentDate (сегодня), когда мы его применяем.
 * Остаются поля: Название, Сумма, Категория (Бюджет), Тип (Расход/Доход).
📋 Чеклист для AI-IDE (Рефакторинг Правил в Шаблоны)
Скопируй это для своей модели. Тут описана логика перехода на Combobox и создание шаблонов "на лету".
Data Structure & Logic
 * [ ] Rename Entity: Переименовать сущность Rules в Templates (или TransactionTemplates) в коде и интерфейсе.
 * [ ] Update Schema: Удалить поле date из схемы Templates. Шаблоны не привязаны к календарным датам.
 * [ ] Limit Check: Реализовать проверку на лимит (макс. 3 шаблона для Free tier).
Transaction Modal (UI Changes)
 * [ ] Autocomplete Input (Combobox):
   * Заменить обычный Input названия на компонент Combobox (или Command из shadcn/ui).
   * Data Source: В список подсказок выводить UserTemplates (с иконкой ⭐) + Историю последних уникальных названий.
   * Item Selection Logic:
     * При клике на элемент из списка Шаблонов: Автоматически заполнять поля Amount, Category, Title, Type значениями из шаблона.
     * Поле Date в модалке оставлять "Сегодня" (не менять).
 * [ ] "Save as Template" Toggle:
   * Добавить компонент Switch или Checkbox внизу формы (перед кнопкой Submit).
   * Label: "Сохранить как шаблон" (Save as template).
   * Logic: Если включено -> При сабмите формы делать два запроса: 1) Создать транзакцию, 2) Создать шаблон (с проверкой лимита).
Settings Page (Management)
 * [ ] Refactor List: Переименовать раздел в "Шаблоны транзакций".
 * [ ] Edit Form: Убрать инпут выбора Даты из формы редактирования шаблона.
User Flow Optimization
 * [ ] Remove Old Logic: Убрать старую логику, когда при полном совпадении имени появлялись кнопки "Подтвердить/Отменить". Теперь автозаполнение происходит мгновенно при выборе из выпадающего списка.
 * [ ] Visual Cue: В выпадающем списке названия шаблонов должны выделяться (например, иконка звезды или жирный шрифт), чтобы отличаться от просто истории ввода.
Пример (Как это будет работать):
 * Юзер открывает модалку.
 * Фокус на сумме. Он его пропускает (или нет).
 * Тапает на Название.
 * Видит список:
   * ⭐ Аренда квартиры (500$)
   * ⭐ Интернет (20$)
   * ... (далее просто история)
 * Выбирает "Интернет".
 * Поле суммы само становится 20, категория "Счета".
 * Жмет "Сохранить". Готово за 2 секунды.