**Роль:** Старший архитектор фронтенда (Next.js 14+, Tailwind CSS, Shadcn UI).

**Контекст:**
Мы проводим рефакторинг главной страницы `DashboardPage` финтех-приложения PWA («Spendly»).

Текущая реализация перегружена огромными вертикальными картами и тяжёлыми диаграммами. Нам необходимо перейти на «удобный» дизайн, ориентированный в первую очередь на мобильные устройства.

**Технические ограничения:**
* **Фреймворк:** Next.js (маршрутизатор приложений).
* **Библиотека пользовательского интерфейса:** shadcn/ui (Radix + Tailwind).
* **Иконки:** lucide-react.
* **Локализация:** Приложение поддерживает 7 языков. **ВСЕ текстовые строки должны использовать ключи перевода** (например, `t('dashboard.greeting')`). Не используйте англоязычные строки в жёстком коде.
* **Тематика:** Должна выглядеть идеально как в тёмном (по умолчанию), так и в светлом режимах. Используйте семантические цвета (`bg-card`, `text-muted-foreground` и т. д.).
* **Маршрутизация:** Чат находится на отдельной странице (`/ai-assistant` или `/chat`), а НЕ в модальном окне.

---

### 🚀 Разбивка задачи

#### 1. Заголовок: Динамическое приветствие
* **Логика:** Отображать приветствие в зависимости от местного времени пользователя:
* Утро (05-12), День (12-18), Вечер (18-05).
* **Содержание:** "{Greeting}, {UserName} 👋".
* **Локализация:** Ключи типа `greeting.morning`, `greeting.afternoon`.

#### 2. Карточки KPI (рефакторинг в `CompactKPICard`)
Текущие карточки слишком высокие. Создайте переработанную версию.
* **Стратегия макета:**
* **Мобильные устройства:** Горизонтальная прокрутка (`flex-row`, `overflow-x-auto`, `snap-x`, `scrollbar-hide`). Карточки должны иметь `min-w-[85vw]` для перехода к следующей.
* **Десктопы:** Система сеток (`grid-cols-3 gap-4`).
* **Карточка 1: Общий бюджет:**
* Отображать: Заголовок, Увеличенную сумму, Тонкую полосу прогресса, текст «Потрачено / Итого».
* *Удалить:* Лишний текст с процентами, если он дублирует визуальную полосу. * **Карточка 2: Общие расходы:**
* Отображать: Заголовок, Большую сумму, Значок тренда (например, «↘ 12%» зелёным/красным цветом).
* *Удалить:* Подробные описания, такие как «по сравнению с прошлым месяцем».
* **Карточка 3: Ежедневные безопасные расходы (НОВОЕ):**
* *Заменить* карточку «Доход» этой метрикой.
* *Формула:* `(Бюджетный лимит - Общие расходы) / Осталось дней в месяце`.
* *Логика:* Если результат < 0, отображать 0 долларов или отрицательную сумму красным текстом.

#### 3. Компонент: `AiInsightTeaser` (Новый отдельный компонент)
Создать отдельный компонент `components/dashboard/AiInsightTeaser.tsx`.
* **Цель:** Мгновенный анализ «поддельного ИИ» с нулевыми затратами с использованием клиентской эвристики (без вызовов API при монтировании).
* **Логика эвристики (проверка по порядку):**
1. **Критическое:** Если любой бюджет израсходован более чем на 90% → вернуть сообщение типа «Оповещение».
2. **Позитивное:** Если недельные расходы < расходов прошлой недели → вернуть сообщение типа «Похвала».
3. **Тихий день:** Если `today_transactions === 0` → вернуть сообщение типа «Экономия».
4. **По умолчанию:** «Проанализируйте свои финансы с помощью Spendly Pal».
* **Взаимодействие:**
* Весь компонент можно кликнуть (`cursor-pointer`).
* **Действие:** `router.push('/ai-assistant')` (Перейти на страницу чата).

* **Дизайн:** Компактный баннер, лёгкая градиентная рамка или `bg-primary/10`, значок `Sparkles`. По возможности анимированный вход.

#### 4. Диаграммы: Динамика расходов
* **Упрощение:** Удалить столбчатые диаграммы «Сравнение бюджетов» (перенести на страницу «Бюджеты»).
* **Сохранить:** Однолинейную **графику** (динамика расходов).
* **Стиль:** Эстетика спарклайна. Скрыть линии сетки (`CartesianGrid`), по возможности скрыть отметки осей, сосредоточиться на кривой.
* **Высота:** Минимальная высота (например, `h-[150px]`).

#### 5. Список недавних действий
* **Данные:** Разбить массив транзакций на срезы, чтобы отобразить только **последние 5 элементов**.
* **Макет:** Использовать упрощённый вид списка (значок + заголовок/дата + сумма). Заголовки таблиц отсутствуют.
* **Нижний/верхний колонтитул:** Добавьте кнопку «Просмотреть все» со ссылкой на `/transactions`.

---

### 📝 Требования к выходным данным
1. Предоставьте код для нового вспомогательного хука `useInsightHeuristics.ts` (для логики тизера).
2. Предоставьте код для компонента `AiInsightTeaser.tsx`.
3. Предоставьте основную структуру макета `DashboardPage.tsx`, интегрирующую эти компоненты.
4. Убедитесь, что классы Tailwind корректно обрабатывают адаптивное переключение (мобильный слайдер и десктопная сетка).