1) Проверить и зафиксировать определение плана подписки — выполнено
   1.1) Файл `src/hooks/useSubscription.ts` определяет `'free' | 'pro'` на основе `user_metadata` и `NEXT_PUBLIC_PREMIUM_EMAILS`.
   1.2) Отметить, что allowlist переопределяет метаданные на Pro (важно для тестов и отписки).

2) Ограничить выбор тона ассистента только для Pro в настройках пользователя — выполнено
   2.1) В `src/components/ai-assistant/ToneSettings.tsx` подключить `useSubscription`.
   2.2) Для `subscriptionPlan === 'free'`:
       - Показать только опцию `neutral`; остальные скрыть или заблокировать.
       - Добавить хинт о Pro-доступности тона и кнопку апгрейда (`/payment`).
       - При инициализации принудительно выбрать `neutral`; не сохранять другие тона в профиль.
   2.3) Для `subscriptionPlan === 'pro'` — оставить доступ ко всем тонам и сохранение в `user_metadata.assistant_tone`.

3) Ограничить выбор тона ассистента только для Pro в окне чата — выполнено
   3.1) В `src/components/ai-assistant/ChatInput.tsx` подключить `useSubscription` и заблокировать `ToneSelect` для Free.
   3.2) В `src/hooks/useChat.ts` принудительно устанавливать `tone`:
       - `tone = assistantTone` для Pro;
       - `tone = 'neutral'` для Free (даже если в метаданных другое значение).
   3.3) При смене плана на Free сбрасывать локальное состояние тона в `'neutral'`; запись в профиль выполнять только для Pro.
   3.4) В `src/hooks/useAISuggestions.ts` аналогично форсировать `tone = 'neutral'` для Free.

4) Добавить секцию "Danger Zone" (только для Pro) в настройках — выполнено
   4.1) Вниз страницы `src/app/[locale]/(protected)/user-settings/UserSettingsClient.tsx` добавить секцию:
       - Заголовок и описание "Danger Zone".
       - Кнопку `Unsubscribe` (вариант `destructive`).
       - Условный рендер только при `subscriptionPlan === 'pro'`.

5) Создать модальное окно подтверждения отписки — выполнено
   5.1) Новый компонент `src/components/modals/UnsubscribeModal.tsx` на базе `Dialog`:
       - Заголовок: подтверждение отписки.
       - Текст-пояснение + эмодзи `😢` для дружелюбного UX.
       - Кнопки: `Cancel` и `Confirm`.

6) Реализовать логику отписки — выполнено
   6.1) По подтверждению в модалке:
       - `supabase.auth.updateUser({ data: { subscription_status: 'free', isPro: false, assistant_tone: 'neutral' } })`.
       - Вызвать `signOut()` из `AuthContext`.
       - Перенаправить на `/auth?tab=signup`.
   6.2) В `src/app/[locale]/(auth)/AuthPageClient.tsx` читать query `tab=signup` и устанавливать активную вкладку `signup`.

7) Локализация — выполнено
   7.1) Добавить ключи для Danger Zone:
       - `userSettings.dangerZone.title`, `userSettings.dangerZone.description`, `userSettings.dangerZone.unsubscribe`.
       - `userSettings.dangerZone.modal.title`, `userSettings.dangerZone.modal.confirm`, `userSettings.dangerZone.modal.cancel`.
   7.2) Для ограничения тона в настройках:
       - `assistant.tone.proOnlyHint` (подсказка для Free).
   7.3) Обновить `en.json`, `ru.json`, `uk.json` и проверить наличие `assistant.settings.title/description`.

8) Проверка сценариев (QA)
   8.1) Free-пользователь:
       - В настройках: видна только `neutral`, отображается Upgrade-CTA.
       - В чате: переключатель тона недоступен; запросы уходят с `tone: 'neutral'`.
   8.2) Pro-пользователь:
       - В настройках: доступны все тона, сохранение в профиль работает.
       - Видна секция Danger Zone; `Unsubscribe` показывает модалку и выполняет отписку.
       - После подтверждения: метаданные — Free, разлогинен, редирект на `/auth?tab=signup`.
   8.3) Allowlist:
       - После отписки для теста убрать email из `NEXT_PUBLIC_PREMIUM_EMAILS`, иначе пользователь останется Pro.

9) Регрессия и совместимость
   9.1) Проверить отсутствие влияния на `e2e/recurring-rules.free.spec.ts`.
   9.2) Визуальная проверка: секция настроек, модалка, окно чата (Free/Pro).
   9.3) Проверить трекеры и лимиты: сообщения AI для Free остаются ограниченными и корректно сигнализируют лимиты.